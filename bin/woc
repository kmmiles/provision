#!/bin/bash

usage() {
  cat << EOF >&2
Usage: $(basename "$0") <WORD> <FILE>

Print occurences of <WORD> in <FILE>
EOF
}

word="${1:-}"
file="${2:-}"

if [[ -z "$word" || -z "$file" ]]; then
  usage
  exit 1
fi

grep -o "$word" "$file" | wc -l
