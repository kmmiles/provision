#!/bin/bash

word="${1:-}"
file="${2:-}"

if [[ -z "$word" || -z "$file" ]]; then
  echo "Usage: $(basename "$0") <word> <file>"
  echo ""
  echo "Print occurences of <word> in <file>"
  exit 1
fi

grep -o "$word" "$file" | wc -l
