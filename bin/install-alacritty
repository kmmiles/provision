#!/bin/bash

#shellcheck source=../lib/provision.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/provision.sh"
lib_init

if $IS_GUI && ! chkpath "alacritty"; then
  ppa_add "ppa:aslatter/ppa"
  sudo apt -y install \
    alacritty
fi
