#!/bin/bash

# shellcheck source=/dev/null
. "$HOME"/.local/lib/bash-tools/lib.sh

if $IS_GUI; then
  log_msg "Installing fonts"
  sudo apt --no-install-recommends -y install \
    fonts-firacode 
fi
